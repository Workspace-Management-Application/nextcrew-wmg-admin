<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Admin Dashboard - Workspace Management" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- Google Fonts - Catamaran and Mukta -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Catamaran:wght@300;400;500;600;700&family=Mukta:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Custom Font Styling -->
    <style>
      /* Apply Catamaran to body and normal text */
      body, .admin-body {
        font-family: 'Catamaran', sans-serif !important;
      }
      
      /* Apply Mukta to headings */
      h1, h2, h3, h4, h5, h6, .sidebar-title {
        font-family: 'Mukta', sans-serif !important;
      }
      
      /* Special styling for NEXTCOWORKS branding */
      .brand-heading {
        font-family: 'Mukta', sans-serif !important;
        font-weight: 700;
        letter-spacing: 3px;
      }
      
      /* Apply Catamaran to sidebar nav items and other text */
      .nav-text, .nav-item, p, span, div, td, th, .btn, .dropdown-item {
        font-family: 'Catamaran', sans-serif;
      }
      
      /* Dark theme pagination styling */
      .pagination {
        --bs-pagination-bg: #2d2d2d;
        --bs-pagination-border-color: #555;
        --bs-pagination-color: #fff;
        --bs-pagination-hover-bg: #404040;
        --bs-pagination-hover-border-color: #666;
        --bs-pagination-hover-color: #fff;
        --bs-pagination-active-bg: #007bff;
        --bs-pagination-active-border-color: #007bff;
        --bs-pagination-active-color: #fff;
        --bs-pagination-disabled-bg: #1a1a1a;
        --bs-pagination-disabled-border-color: #333;
        --bs-pagination-disabled-color: #666;
      }
      
      .pagination .page-link {
        background-color: var(--bs-pagination-bg);
        border-color: var(--bs-pagination-border-color);
        color: var(--bs-pagination-color);
      }
      
      .pagination .page-link:hover {
        background-color: var(--bs-pagination-hover-bg);
        border-color: var(--bs-pagination-hover-border-color);
        color: var(--bs-pagination-hover-color);
      }
      
      .pagination .page-item.active .page-link {
        background-color: var(--bs-pagination-active-bg);
        border-color: var(--bs-pagination-active-border-color);
        color: var(--bs-pagination-active-color);
      }
      
      .pagination .page-item.disabled .page-link {
        background-color: var(--bs-pagination-disabled-bg);
        border-color: var(--bs-pagination-disabled-border-color);
        color: var(--bs-pagination-disabled-color);
      }
      
      /* Clickable row styling */
      .clickable-row:hover {
        background-color: #404040 !important;
      }
      
      .action-column {
        pointer-events: all;
      }
      
      /* Fix sidebar header alignment */
      .sidebar-header {
        display: flex !important;
        align-items: center !important;
        justify-content: space-between !important;
        padding: 1rem;
        min-height: 60px;
      }
      
      .sidebar-title {
        margin-bottom: 0 !important;
        line-height: 1.5;
      }
      
      .sidebar-toggle {
        padding: 0.25rem 0.5rem !important;
        line-height: 1 !important;
        border: 1px solid rgba(255,255,255,0.3) !important;
      }
    </style>
    
    <!-- Chart.js -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    
    <%= stylesheet_link_tag 'application', 'data-turbo-track': 'reload' %>
    <%= javascript_importmap_tags %>
    <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
    <!-- Bootstrap Select CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-select@1.14.0-beta3/dist/css/bootstrap-select.min.css">
  </head>

  <body class="admin-body">
    <!-- Sidebar -->
    <div class="sidebar show" id="sidebar">
      <div class="sidebar-header d-flex align-items-center justify-content-between">
        <h4 class="sidebar-title mb-0"><%= format_role_display(current_user.role) %> Panel</h4>
        <button class="sidebar-toggle btn btn-outline-light ms-2" onclick="toggleSidebar()" style="font-size: 1.5rem;">
          ☰
        </button>
      </div>
      
      <nav class="sidebar-nav">
        <%= link_to admin_dashboard_path, class: "nav-item #{'active' if current_page?(admin_dashboard_path)}" do %>
          <span class="nav-icon">📊</span>
          <span class="nav-text">Dashboard</span>
        <% end %>
        
        <%= link_to admin_workspaces_path, class: "nav-item #{'active' if controller_name == 'workspaces'}" do %>
          <span class="nav-icon">🏢</span>
          <span class="nav-text">Workspaces</span>
        <% end %>
        
        <%= link_to admin_rooms_path, class: "nav-item #{'active' if controller_name == 'rooms'}" do %>
          <span class="nav-icon">🚪</span>
          <span class="nav-text">Rooms</span>
        <% end %>
        
        <%= link_to admin_bookings_path, class: "nav-item #{'active' if controller_name == 'bookings'}" do %>
          <span class="nav-icon">📅</span>
          <span class="nav-text">Bookings</span>
        <% end %>
        
        <%= link_to admin_users_path, class: "nav-item #{'active' if controller_name == 'users'}" do %>
          <span class="nav-icon">👤</span>
          <span class="nav-text">Users</span>
        <% end %>
        
        <%= link_to admin_companies_path, class: "nav-item #{'active' if controller_name == 'companies'}" do %>
          <span class="nav-icon">🏭</span>
          <span class="nav-text">Companies</span>
        <% end %>
        
        <%= link_to admin_office_enquiries_path, class: "nav-item #{'active' if controller_name == 'office_enquiries'}" do %>
          <span class="nav-icon">📋</span>
          <span class="nav-text">Office Enquiries</span>
        <% end %>
        
        <%= link_to admin_visitor_entries_path, class: "nav-item #{'active' if controller_name == 'visitor_entries'}" do %>
          <span class="nav-icon">👥</span>
          <span class="nav-text">Visitor Entries</span>
        <% end %>
        
        <%= link_to admin_workspace_types_path, class: "nav-item #{'active' if controller_name == 'workspace_types'}" do %>
          <span class="nav-icon">⚙️</span>
          <span class="nav-text">Workspace Types</span>
        <% end %>
        
        <%= link_to admin_amenities_path, class: "nav-item #{'active' if controller_name == 'amenities'}" do %>
          <span class="nav-icon">🛠️</span>
          <span class="nav-text">Amenities</span>
        <% end %>
        
        <%= link_to admin_exports_path, class: "nav-item #{'active' if controller_name == 'exports'}" do %>
          <span class="nav-icon">📊</span>
          <span class="nav-text">Export</span>
        <% end %>
      </nav>
    </div>

    <!-- Floating open sidebar button -->
    <button id="openSidebarBtn" class="btn btn-outline-light position-fixed" style="top: 20px; left: 10px; z-index: 2000; display: none; font-size: 1.5rem;" onclick="toggleSidebar()">
      ☰
    </button>

    <!-- Main Content -->
    <div class="main-content">
      <!-- Top Header -->
      <header class="top-header d-flex align-items-center" style="min-height: 120px; background: #000 !important; color: #fff !important; position: relative;">
        <div class="flex-grow-1 text-center">
          <img src="https://meetingroombookingbucket.s3.amazonaws.com/New-Project.png" alt="NextCoworks Logo" style="max-height: 60px; margin-bottom: 0.5rem;">
          <hr style="width: 60px; margin: 0.5rem auto; border-top: 2px solid #fff; opacity: 1;">
          <div style="font-family: 'Catamaran', sans-serif; font-size: 1rem;">Coworking Spaces | Private Cabin | Private Office | Private Desk | Shared Desk</div>
        </div>
        <!-- Profile Dropdown -->
        <div class="profile-dropdown position-absolute end-0 me-4">
          <button class="profile-btn" data-bs-toggle="dropdown" aria-expanded="false" id="profileDropdownBtn" data-turbo="true">
            <div class="profile-avatar">
              <%= current_user.name&.first&.upcase || "A" %>
            </div>
          </button>
          <ul class="dropdown-menu dropdown-menu-end" id="profileDropdownMenu">
            <li><%= link_to "Edit Profile", admin_profile_path, class: "dropdown-item" %></li>
            <li><hr class="dropdown-divider"></li>
            <li><%= button_to "Logout", destroy_user_session_path, method: :delete, class: "dropdown-item border-0 bg-transparent w-100 text-start", form: { style: "display: inline;" } %></li>
          </ul>
        </div>
      </header>

      <!-- Content Area -->
      <main class="content-area">
        <% if notice %>
          <div class="alert alert-success alert-dismissible fade show" role="alert">
            <%= notice %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
        <% end %>

        <% if alert %>
          <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <%= alert %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
        <% end %>

        <%= yield %>
      </main>
    </div>

    <%= yield :scripts %>
    
    <!-- jQuery (required for Bootstrap Select) -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Bootstrap Select JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap-select@1.14.0-beta3/dist/js/bootstrap-select.min.js"></script>
    
    <script>
      function toggleSidebar() {
        const sidebar = document.getElementById('sidebar');
        sidebar.classList.toggle('show');
      }

      // Initialize Bootstrap components
      function initializeBootstrapComponents() {
        // Make sure Bootstrap is available
        if (typeof bootstrap === 'undefined') {
          console.warn('Bootstrap not loaded yet, retrying in 100ms...');
          setTimeout(initializeBootstrapComponents, 100);
          return;
        }

        // Initialize all dropdowns
        const dropdownElementList = document.querySelectorAll('[data-bs-toggle="dropdown"]');
        dropdownElementList.forEach(function(dropdownToggleEl) {
          try {
            // Remove any existing dropdown instance
            const existingDropdown = bootstrap.Dropdown.getInstance(dropdownToggleEl);
            if (existingDropdown) {
              existingDropdown.dispose();
            }
            
            // Create new dropdown instance
            const dropdown = new bootstrap.Dropdown(dropdownToggleEl);
            console.log('Dropdown initialized for:', dropdownToggleEl);
          } catch (error) {
            console.error('Error initializing dropdown:', error);
          }
        });

        // Initialize all tooltips
        const tooltipTriggerList = document.querySelectorAll('[data-bs-toggle="tooltip"]');
        tooltipTriggerList.forEach(function(tooltipTriggerEl) {
          try {
            const existingTooltip = bootstrap.Tooltip.getInstance(tooltipTriggerEl);
            if (existingTooltip) {
              existingTooltip.dispose();
            }
            const tooltip = new bootstrap.Tooltip(tooltipTriggerEl);
          } catch (error) {
            console.error('Error initializing tooltip:', error);
          }
        });

        // Initialize all popovers
        const popoverTriggerList = document.querySelectorAll('[data-bs-toggle="popover"]');
        popoverTriggerList.forEach(function(popoverTriggerEl) {
          try {
            const existingPopover = bootstrap.Popover.getInstance(popoverTriggerEl);
            if (existingPopover) {
              existingPopover.dispose();
            }
            const popover = new bootstrap.Popover(popoverTriggerEl);
          } catch (error) {
            console.error('Error initializing popover:', error);
          }
        });
      }

      // Handle clickable table rows
      function initializeClickableRows() {
        const clickableRows = document.querySelectorAll('.clickable-row');
        
        clickableRows.forEach(function(row) {
          // Remove any existing listeners to avoid duplicates
          row.removeEventListener('click', handleRowClick);
          
          // Add the click listener
          row.addEventListener('click', handleRowClick);
        });
      }
      
      function handleRowClick(e) {
        // Don't navigate if clicking on action buttons, links, or form elements
        if (e.target.closest('.action-column') || 
            e.target.closest('.btn') || 
            e.target.closest('a') ||
            e.target.closest('button') ||
            e.target.closest('form') ||
            e.target.closest('input') ||
            e.target.closest('select')) {
          return;
        }
        
        const href = this.getAttribute('data-href');
        if (href) {
          window.location.href = href;
        }
      }

      // Initialize Bootstrap Select
      function initializeBootstrapSelect() {
        if (typeof $ !== 'undefined' && $.fn.selectpicker) {
          $('.selectpicker').selectpicker();
          console.log('Bootstrap Select initialized');
        } else {
          console.warn('Bootstrap Select not available, retrying in 100ms...');
          setTimeout(initializeBootstrapSelect, 100);
        }
      }
        
      // Initialize everything on page load
      document.addEventListener('DOMContentLoaded', function() {
        console.log('DOMContentLoaded - Initializing components...');
        initializeBootstrapComponents();
        initializeClickableRows();
        initializeBootstrapSelect();
      });
        
      // Re-initialize after Turbo navigation
      document.addEventListener('turbo:load', function() {
        console.log('turbo:load - Re-initializing components...');
        initializeBootstrapComponents();
        initializeClickableRows();
        initializeBootstrapSelect();
      });

      // Also initialize immediately if DOM is already loaded
      if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', function() {
          console.log('DOMContentLoaded (delayed) - Initializing components...');
          initializeBootstrapComponents();
          initializeClickableRows();
          initializeBootstrapSelect();
        });
      } else {
        console.log('DOM already loaded - Initializing components immediately...');
        initializeBootstrapComponents();
        initializeClickableRows();
        initializeBootstrapSelect();
      }

      // Additional initialization after a short delay to ensure everything is loaded
      setTimeout(function() {
        console.log('Delayed initialization - Ensuring components are ready...');
        initializeBootstrapComponents();
        initializeBootstrapSelect();
      }, 500);

      // Manual fallback for profile dropdown
      function initializeProfileDropdown() {
        const profileBtn = document.getElementById('profileDropdownBtn');
        const profileMenu = document.getElementById('profileDropdownMenu');
        
        if (profileBtn && profileMenu) {
          // Remove existing listeners
          profileBtn.removeEventListener('click', toggleProfileDropdown);
          
          // Add manual click handler as fallback
          profileBtn.addEventListener('click', toggleProfileDropdown);
          
          // Close dropdown when clicking outside
          document.addEventListener('click', function(event) {
            if (!profileBtn.contains(event.target) && !profileMenu.contains(event.target)) {
              profileMenu.classList.remove('show');
              profileBtn.setAttribute('aria-expanded', 'false');
            }
          });
        }
      }
      
      function toggleProfileDropdown(event) {
        event.preventDefault();
        event.stopPropagation();
        console.log("hello12")
        const profileMenu = document.getElementById('profileDropdownMenu');
        const profileBtn = document.getElementById('profileDropdownBtn');
        
        if (profileMenu && profileBtn) {
          const isVisible = profileMenu.classList.contains('show');
          
          if (isVisible) {
            profileMenu.classList.remove('show');
            profileBtn.setAttribute('aria-expanded', 'false');
          } else {
            profileMenu.classList.add('show');
            profileBtn.setAttribute('aria-expanded', 'true');
          }
        }
      }
    </script>
  </body>
</html>
