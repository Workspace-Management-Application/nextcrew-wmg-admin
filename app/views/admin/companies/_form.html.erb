<%= form_with model: [:admin, @company], local: true, class: "needs-validation", novalidate: true, data: { turbo: false } do |form| %>
  <% if @company.errors.any? %>
    <div class="alert alert-danger">
      <h6><%= pluralize(@company.errors.count, "error") %> prohibited this company from being saved:</h6>
      <ul class="mb-0">
        <% @company.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="col-md-6 mb-3">
      <%= form.label :name, class: "form-label" do %>
        Company Name <span class="text-danger">*</span>
      <% end %>
      <%= form.text_field :name, class: "form-control", required: true %>
    </div>
    
    <div class="col-md-6 mb-3">
      <%= form.label :email, class: "form-label" do %>
        Email <span class="text-danger">*</span>
      <% end %>
      <%= form.email_field :email, class: "form-control", required: true %>
    </div>
  </div>

  <!-- Status field - only visible to super_admin -->
  <% if @company.persisted? %>
    <% if current_user.super_admin? %>
      <div class="row">
        <div class="col-md-6 mb-3">
          <%= form.label :status, class: "form-label" %>
          <%= form.select :status, 
              options_for_select([
                ['Active', 'active'],
                ['Inactive', 'inactive']
              ], @company.status), 
              {}, 
              { class: "select2-select", 'data-placeholder': 'Select Status' } %>
        </div>
      </div>
    <% else %>
      <!-- Hidden field to preserve current status -->
      <%= form.hidden_field :status %>
      <div class="row">
        <div class="col-md-6 mb-3">
          <label class="form-label">Current Status</label>
          <div class="form-control-plaintext">
            <span class="badge <%= @company.status == 'active' ? 'bg-success' : 'bg-secondary' %>">
              <%= @company.status.titleize %>
            </span>
            <small class="form-text text-muted d-block mt-1">Only Super Admin can change this status</small>
          </div>
        </div>
      </div>
    <% end %>
  <% end %>
    
  <div class="row">
    <div class="col-md-6 mb-3">
      <%= form.label :phone_number, class: "form-label" do %>
        Phone Number <span class="text-danger">*</span>
      <% end %>
      <%= form.telephone_field :phone_number, class: "form-control", required: true %>
    </div>
    <div class="col-md-6 mb-3">
      <%= form.label :cabin_number, class: "form-label" do %>
        Cabin Number <span class="text-danger">*</span>
      <% end %>
      <%= form.text_field :cabin_number, class: "form-control", required: true %>
    </div>
  </div>

  <div class="row">
    <div class="col-md-6 mb-3">
      <%= form.label :workspace_id, "Workspace", class: "form-label" %><span class="text-danger">*</span>
      <%= form.select :workspace_id,
        options_from_collection_for_select(@workspaces, :id, :name, @company.workspaces.first&.id),
        { prompt: 'Select Workspace' },
        { class: "select2-select", id: "company_workspace_id", 'data-placeholder': 'Select Workspace' } %>
    </div>
    <div class="col-md-6 mb-3">
      <%= form.label :room_ids, "Rooms", class: "form-label" %>
      <% if @company.persisted? %>
        <%= form.collection_select :room_ids, @rooms, :id, :name, { selected: @company.rooms.pluck(:id), multiple: true }, { class: "select2-select", multiple: true, required: true, 'data-placeholder': "Select Rooms", id: "company_room_ids" } %>
      <% else %>
        <%= form.collection_select :room_ids, [], :id, :name, { multiple: true }, { class: "select2-select", multiple: true, required: true, 'data-placeholder': "Select Rooms", id: "company_room_ids" } %>
      <% end %>
    </div>
  </div>

  <div class="row">
    <div class="col-md-6 mb-3">
      <%= form.label :no_of_employee, class: "form-label" do %>
        Number of Employees <span class="text-danger">*</span>
      <% end %>
      <%= form.number_field :no_of_employee, class: "form-control", required: true, min: 1 %>
    </div>
    <div class="col-md-6 mb-3">
      <%= form.label :meeting_time_limit_per_month, class: "form-label" do %>
        Meeting Time Limit per Month (minutes) <span class="text-danger">*</span>
      <% end %>
      <%= form.number_field :meeting_time_limit_per_month, class: "form-control", required: true, min: 1 %>
    </div>
  </div>

  <div class="row">
    <div class="col-md-3 mb-3">
      <%= form.label :no_of_meeting_per_day, class: "form-label" do %>
        Number of Meetings per Day <span class="text-danger">*</span>
      <% end %>
      <%= form.number_field :no_of_meeting_per_day, class: "form-control", required: true, min: 1 %>
    </div>
    <div class="col-md-3 mb-3">
      <%= form.label :meeting_time_limit_per_day, class: "form-label" do %>
        Daily Meeting Time Limit (minutes) <span class="text-danger">*</span>
      <% end %>
      <%= form.number_field :meeting_time_limit_per_day, class: "form-control", required: true, min: 1 %>
    </div>
    <div class="col-md-3 mb-3">
      <%= form.label :minimum_minutes_meeting_limit, class: "form-label" do %>
        Minimum Meeting Duration (minutes) <span class="text-danger">*</span>
      <% end %>
      <%= form.number_field :minimum_minutes_meeting_limit, class: "form-control", required: true, min: 1 %>
    </div>
    <div class="col-md-3 mb-3">
      <%= form.label :max_minutes_meeting_limit, class: "form-label" do %>
        Maximum Meeting Duration (minutes) <span class="text-danger">*</span>
      <% end %>
      <%= form.number_field :max_minutes_meeting_limit, class: "form-control", required: true, min: 1 %>
    </div>
  </div>

  <div class="mb-3">
    <div class="form-check">
      <%= form.check_box :parallel_booking, class: "form-check-input" %>
      <%= form.label :parallel_booking, "Allow Parallel Booking", class: "form-check-label" %>
    </div>
  </div>


  <!-- New Company Fields Section -->
  <h5 class="mt-4 mb-3">Company Details</h5>
  <div class="row">
    <div class="mb-3">
      <%= form.label :address, class: "form-label" do %>
        Company Registered Address <span class="text-danger">*</span>
      <% end %>
      <%= form.text_area :address, class: "form-control", rows: 3, required: true %>
    </div>
  </div>
  <div class="row">
    <div class="col-md-6 mb-3">
      <%= form.label :centre_address, class: "form-label" do %>
        Company Centre Address <span class="text-danger">*</span>
      <% end %>
      <%= form.text_area :centre_address, class: "form-control", rows: 2, required: true %>
    </div>
    <div class="col-md-6 mb-3">
      <%= form.label :company_poc_contact, class: "form-label" do %>
        Company POC Contact <span class="text-danger">*</span>
      <% end %>
      <%= form.text_field :company_poc_contact, class: "form-control", required: true %>
    </div>
  </div>

  <div class="row">
    <div class="col-md-6 mb-3">
      <%= form.label :company_email_address, class: "form-label" do %>
        Company Email Address <span class="text-danger">*</span>
      <% end %>
      <%= form.email_field :company_email_address, class: "form-control", required: true %>
    </div>
    <div class="col-md-6 mb-3">
      <%= form.label :floor_no, class: "form-label" do %>
        Floor No <span class="text-danger">*</span>
      <% end %>
      <%= form.text_field :floor_no, class: "form-control", required: true %>
    </div>
  </div>

  <div class="row">
    <div class="col-md-4 mb-3">
      <%= form.label :no_of_seats, class: "form-label" do %>
        No of Seats <span class="text-danger">*</span>
      <% end %>
      <%= form.number_field :no_of_seats, class: "form-control", required: true, min: 1 %>
    </div>
    <div class="col-md-4 mb-3">
      <%= form.label :lock_in_period, class: "form-label" do %>
        Lock-in Period (months) <span class="text-danger">*</span>
      <% end %>
      <%= form.number_field :lock_in_period, class: "form-control", required: true, min: 1 %>
    </div>
    <div class="col-md-4 mb-3">
      <%= form.label :cost_per_seat, class: "form-label" do %>
        Cost per Seat / Monthly Rental <span class="text-danger">*</span>
      <% end %>
      <%= form.number_field :cost_per_seat, class: "form-control", required: true, min: 1 %>
    </div>
  </div>

  <div class="row">
    <div class="col-md-4 mb-3">
      <%= form.label :commencement_date, class: "form-label" do %>
        Commencement Date <span class="text-danger">*</span>
      <% end %>
      <%= form.date_field :commencement_date, class: "form-control", required: true %>
    </div>
    <div class="col-md-4 mb-3">
      <%= form.label :agreement_date, class: "form-label" do %>
        Agreement Date <span class="text-danger">*</span>
      <% end %>
      <%= form.date_field :agreement_date, class: "form-control", required: true %>
    </div>
    <div class="col-md-4 mb-3">
      <%= form.label :lease_expiry_date, class: "form-label" do %>
        Lease Expiry Date <span class="text-danger">*</span>
      <% end %>
      <%= form.date_field :lease_expiry_date, class: "form-control", required: true %>
    </div>
  </div>

  <div class="row">
    <div class="col-md-6 mb-3">
      <%= form.label :shift_type, class: "form-label" do %>
        Shift Type <span class="text-danger">*</span>
      <% end %>
      <%= form.select :shift_type, 
          options_for_select(Company::SHIFT_TYPES, @company.shift_type), 
          { prompt: 'Select Shift Type' }, 
          { class: "form-select", required: true } %>
    </div>
    <div class="col-md-6 mb-3">
      <label class="form-label">Name Board Charges</label>
      <div class="form-check">
        <%= form.radio_button :name_board_charges, true, class: "form-check-input" %>
        <%= form.label :name_board_charges_true, "YES", class: "form-check-label" %>
      </div>
      <div class="form-check">
        <%= form.radio_button :name_board_charges, false, class: "form-check-input" %>
        <%= form.label :name_board_charges_false, "NO", class: "form-check-label" %>
      </div>
    </div>
  </div>

  <div class="d-flex gap-2">
    <%= form.submit @company.persisted? ? "Update Company" : "Create Company", class: "btn btn-primary" %>
    <%= link_to "Cancel", request.referer || (@company.persisted? ? admin_company_path(@company) : admin_companies_path), class: "btn btn-secondary" %>
  </div>
<% end %>

<script>
document.addEventListener('DOMContentLoaded', function() {
  initializeCompanyForm();
});

document.addEventListener('turbo:load', function() {
  initializeCompanyForm();
});

function initializeCompanyForm() {
  const workspaceSelect = document.getElementById('company_workspace_id');
  const roomSelect = document.getElementById('company_room_ids');
  
  if (!workspaceSelect || !roomSelect) return;

  // Initialize Select2 for workspace select
  if (typeof $ !== 'undefined' && $.fn.select2) {
    $(workspaceSelect).select2({
      theme: 'default',
      width: '100%',
      placeholder: 'Select Workspace',
      allowClear: true
    });
  }

  // Initialize Select2 for room select
  if (typeof $ !== 'undefined' && $.fn.select2) {
    $(roomSelect).select2({
      theme: 'default',
      width: '100%',
      placeholder: 'Select Rooms',
      allowClear: true
    });
  }

  function loadRoomsForWorkspace(workspaceId) {
    if (!workspaceId) {
      roomSelect.innerHTML = '';
      if (typeof $ !== 'undefined' && $.fn.select2) {
        $(roomSelect).select2('destroy');
        $(roomSelect).select2({
          theme: 'default',
          width: '100%',
          placeholder: 'Select Rooms',
          allowClear: true
        });
      }
      return;
    }

    fetch(`/admin/companies/rooms_for_workspace?workspace_id=${workspaceId}`)
      .then(response => response.json())
      .then(data => {
        roomSelect.innerHTML = '';
        data.rooms.forEach(room => {
          const option = document.createElement('option');
          option.value = room.id;
          option.textContent = room.name;
          roomSelect.appendChild(option);
        });
        // Reinitialize the Select2 after all options are added
        if (typeof $ !== 'undefined' && $.fn.select2) {
          $(roomSelect).select2('destroy');
          $(roomSelect).select2({
            theme: 'default',
            width: '100%',
            placeholder: 'Select Rooms',
            allowClear: true
          });
        }
      })
      .catch(error => {
        console.error('Error loading rooms:', error);
        roomSelect.innerHTML = '<option value="">Error loading rooms</option>';
        if (typeof $ !== 'undefined' && $.fn.select2) {
          $(roomSelect).select2('destroy');
          $(roomSelect).select2({
            theme: 'default',
            width: '100%',
            placeholder: 'Select Rooms',
            allowClear: true
          });
        }
      });
  }

  // Event listener for workspace change using jQuery for Select2
  if (typeof $ !== 'undefined' && $.fn.select2) {
    $(workspaceSelect).on('change', function() {
      loadRoomsForWorkspace(this.value);
    });
  } else {
    // Fallback to native event listener
    workspaceSelect.addEventListener('change', function() {
      loadRoomsForWorkspace(this.value);
    });
  }
}
</script>